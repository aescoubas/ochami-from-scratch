# Boot artifacts (large binaries, downloaded or built separately)
initramfs-lts
rootfs.squashfs
vmlinuz-lts
boot_tmp/
modules_tmp/
usr_modules_tmp/

# Hardware node inventory (user-specific)
nodes.csv

# Docker Compose runtime files (secrets, override)
deploy/compose/.env
deploy/compose/docker-compose.override.yml

# Boot artifacts cached by build-boot-artifacts.sh
deploy/compose/artifacts/

# Locally generated secrets used by deploy scripts
.openchami-secrets.env

# Apply command state tracking
.openchami-state.yaml

# Libvirt VM test state
libvirt/.state/

# Python
__pycache__/
.pytest_cache/
*.pyc
*.egg-info/
.venv/

# Build output
result
.tmp/

# RPM build artifacts
*.rpm
.rpm-staging/
.ochami-cli/
